// Test special blobmsg types: array, unspec, and table (custom types)

custom_table_type: {
    field: int32
}

object special_types_test {
    // Test array type
    method array(array_val: array)

    // Test unspec type
    method unspec(unspec_val: unspec)

    // Test table type (custom type)
    method table(table_val: custom_table_type)

    // Test all special types together
    method all_special(array_val: array, unspec_val: unspec, table_val: custom_table_type)
}
